# Nginx for Security-Agent Frontend
# - 提供 React 静态文件(SPA history fallback -> index.html)
# - /api 反代到 gateway(容器名 secagent-api:8000,经 compose extra_hosts 注入 hosts)
# - /healthz 容器内自检
#
# 阶段 5 拆分注意:gateway 容器名为 secagent-gateway,而 nginx.conf upstream 仍写
# secagent-api:8000;这是为了避免改 SPA 配置 + nginx 配置。compose 文件负责:
#   - host 版:    frontend.extra_hosts: "secagent-api:127.0.0.1"
#   - bridge 版:  frontend.extra_hosts: "secagent-api:secagent-gateway"
# nginx 启动时 hosts 文件即有 secagent-api 映射,运行期转发直接走 IP,无 DNS 依赖。
# 注意:nginx upstream 默认缓存解析结果,gateway 重启换 IP 时 nginx 仍连旧 IP;
# 触发 reload(nginx -s reload)或重启 frontend 容器即可恢复。

upstream secagent_api {
    server secagent-api:8000;
    keepalive 32;
}

server {
    listen 80 default_server;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 客户端上传的最大体积(配合前端大文件 / 报告导出)
    client_max_body_size 50m;

    # gzip
    gzip on;
    gzip_min_length 1k;
    gzip_types text/plain text/css application/json application/javascript
               application/xml application/xml+rss application/atom+xml
               image/svg+xml application/wasm;

    # 反向代理 /api -> 后端
    location /api/ {
        proxy_pass         http://secagent_api;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }

    # WebSocket(/api/v1/agents/ws)
    location /api/v1/agents/ws {
        proxy_pass         http://secagent_api;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_set_header   Host              $host;
        proxy_read_timeout 86400s;
    }

    # SSE(/api/v1/events/.../stream 等长连接)
    location ~ ^/api/v1/.*/stream$ {
        proxy_pass         http://secagent_api;
        proxy_http_version 1.1;
        proxy_set_header   Connection        "";
        proxy_buffering    off;
        proxy_cache        off;
        proxy_read_timeout 86400s;
    }

    # 容器自检
    location = /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    # SPA history fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源长缓存
    location ~* \.(?:js|css|woff2?|ttf|otf|eot|svg|png|jpg|jpeg|gif|webp|ico)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
        try_files $uri =404;
    }

    # 安全 header
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
}
