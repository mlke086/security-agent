# 后续修复计划 V11（基于项目审查 V11，2026-08-01 综合复审）

> 本计划对 V11 综合复审发现**统一去重**后分批（两轴重叠项见审查报告「五、两轴重叠项」，计一次）。覆盖范围：当前工作树中尚未交付的 P1/P2，含 V11 新增发现 + V10 留存 P1/P2（V10 阶段 1/2 nuclei 链 + Go agent 共 14 项）。
>
> **审查基线**：e3c84afa（V10 stage 4 交付记录）。V10 stages 1-4 全部驻留；V10 nuclei 链 P1 中仅 1 项（fire-and-forget）已 FIXED，其余 8 项 + Go 4 项 P1 + 5 项 P2 全部移交 V11。本计划仅覆盖这些未交付项。
>
> **方法**：一次提交一个 P1（或一组强相关 P2），配独立测试；提交前跑「P0 切片」（取消链路 + 升级链路 + ES trace + nuclei 模板库 + aggregate reconcile）确认无回归。
> 不在没有测试保护的地方改历史 P0 链路。Go 修改配 go vet + 对应 *_test.go 用例；前端修改配
px tsc --noEmit 类型检查。

---

## 零、实施原则

- **优先级**：先关 V10 留存的 nuclei / Go 核心安全 P1（影响所有用户），再关 V11 新引入的 chat / port discovery / nuclei_templates P1，最后是 P2 与测试覆盖。
- **可合并节点**：
  - 阶段 1 全绿 → P0/P1 全部驻留，可合并主干
  - 阶段 1-2 全绿 → nuclei 链整体加固完成
  - 阶段 1-3 全绿 → 全栈一致，可发布候选
  - 阶段 1-4 全绿 → 测试覆盖达 80% + 关键链路有测试，可正式发布
- **每修一项**：更新审查报告对应条目状态（FIXED / 部分 / 回归），保留 commit 与文档对应。
- **SPEC 偏离**：阶段 1 完成后，运行完整 E2E（	ests/e2e/test_scenarios.py）确认 nuclei_templates / portdiscovery / aggregate 全部行为符合预期。

---

## 一、阶段 1：安全与可靠性 P1（阻塞合并，6-9h）

### 1.1 [Spec-P1-UX / S-P1-4] chat._classify 区分异常与低置信度（1h）

- src/api/routers/chat.py:91-105：当前所有异常降级为 intent="chat", confidence=0.0，用户高危操作（隔离主机、删除告警）可借模型异常绕过 ScanIntent 路由。
- **修法**：
  - 在 IntentDecision 加 degraded: bool 字段。
  - _classify 仅在"模型返回 200 但 confidence<0.3"时降级为 chat；其他异常（ModelNotFoundError、timeout、parse）抛 HTTPException(503, "分类器暂不可用")。
  - 异常时写 audit chat_classify_degraded。
  - 前端 ChatScan.tsx 捕获 503 显示「分类暂不可用，请走具体功能页」。
- **测试**：	est_chat_persistence.py 加 	est_classify_503_on_model_not_found + 	est_classify_503_on_timeout。

### 1.2 [S-P1-6 / Spec-P1-EDIT]
uclei_templates 上传/sync 大小与 YAML 校验（1.5h）

- src/api/routers/nuclei_templates.py:73-115：
  - pi_import_zip 加 MAX_UPLOAD_BYTES = 500 * 1024 * 1024，Content-Length 超限 413。
  - parse_templates_zip 加 MAX_ITEMS = 50000 + MAX_UNCOMPRESSED_BYTES = 500MB。
  - pi_save_template 在 save_template 入口 yaml.safe_load 校验，失败 HTTPException(422, "invalid YAML")。
  - pi_sync_from_mirror 拉到的 zip 在 _ingest 前看 len(zip_bytes)，超 500MB 拒绝 + audit oversized_templates_zip。
- **测试**：	est_nuclei_templates.py 加 	est_import_zip_oversized_rejected + 	est_save_invalid_yaml_rejected + 	est_sync_oversized_zip_rejected。

### 1.3 [S-P1-3] portdiscovery.go 失败时返回 error + 推送 scan_step（1h）

- gent/internal/scan/nuclei/portdiscovery.go:24-32：当前 ss + /proc/net/tcp 都失败时静默返回 nil，runner 把 nil 当空 slice，0 findings 被报告为"扫描完成无漏洞"。
- **修法**：
  - 改签名 discoverListeningPorts() ([]string, error)，错误时 eturn nil, err。
  - unner.go:resolveTargetPorts 在 err != nil 时 publish 一条 scan_step 事件，状态 reason = "port_discovery_failed: " + err.Error()。
  - 加 portdiscovery_test.go：mock ss 失败 + /proc/net/tcp 失败，断言返回 error；mock ss 成功，断言 port 列表。
- **测试**：portdiscovery_test.go（新增）含 TestDiscoverListeningPortsSSSuccess / TestDiscoverListeningPortsProcNetSuccess / TestDiscoverListeningPortsBothFail / TestParseSSPorts / TestParseProcNetTCP。

### 1.4 [S-P1-1]
odes._pub_progress 共享 Redis 客户端（0.5h）

- src/orchestration/subgraphs/vulnscan/nodes.py:380-385：每次进度推送都 rom_url + close，180 次/任务连接抖动。
- **修法**：
  - 模块级懒加载 _redis: aioredis.Redis | None = None，_get_redis() helper（与 ggregate 节点 _write_fallback 模式一致）。
  - _pub_progress 改用 _get_redis()，进程退出时 lifespan shutdown 关闭。
  - generate_report 末段 .delete(...) 同样复用。
- **测试**：	est_vulnscan_nodes.py 加 	est_pub_progress_uses_shared_redis（monkeypatch _get_redis 计数调用）。

### 1.5 [G-P1-1 / Spec-P1-DOS] Go nuclei 下载加 size limit（1.5h）

- gent/internal/updater/upgrade.go 三处 io.ReadAll 替换为 io.LimitReader：
  - :217 HandleNucleiUpgrade → 200MB
  - :263 HandleNucleiTemplatesUpdate → 500MB（templates 包大）
  - :384 HandleRuleUpdate → 100MB（rule pack 实际 ~1MB，限 100MB 留余地）
- **修法**：io.LimitReader(resp.Body, maxBytes) + io.ReadAll；超限 io.ReadAll 返回 unexpected EOF / 自定义错误。
- **测试**：upgrade_test.go 加 TestNucleiUpgradeRejectsOversizedZip + TestNucleiTemplatesUpdateRejectsOversized + TestRuleUpdateRejectsOversizedPack（mock HTTP server 返回超过限制的 body）。

### 1.6 [G-P1-2] httpClient(caPath) CA 加载失败改硬错（0.5h）

- gent/internal/updater/upgrade.go:537-555：当前 caPath != "" 时加载失败静默退回系统根 CA。
- **修法**：改签名 httpClient(caPath string) (*http.Client, error)；caPath 非空但加载失败返回
il, err；HandleNucleiUpgrade / HandleNucleiTemplatesUpdate / HandleRuleUpdate 调用处处理 err（SendAck false + return）。
- **测试**：upgrade_test.go 加 TestHTTPClientCAFailHard（不存在 ca 文件 → 返回 error）。

### 1.7 [G-P1-3] agent upgrade 不发双 ack（0.5h）

- gent/cmd/agent/main.go:266-275：当前先发 success ack → goroutine restart → 失败再发 failure ack；服务端 ecord_upgrade_ack 把 state 反复改。
- **修法**：失败时只 log.Printf("[agent] restart failed: %v", err)，不发 second ack；依赖心跳版本 mismatch 触发 re-push。
- **测试**：upgrade_test.go 加 TestUpgradeRestartFailureNoSecondAck（mock ApplyStagedAndRestart 返回 error，断言只发了一次 success ack）。

### 1.8 [S-P1-5] httpx proxy= 版本兼容（0.5h）

- src/agents/chat_search/web_search.py:268-273 + src/agents/rules_sync.py 等所有 httpx.AsyncClient(proxy=...) 调用点：httpx 0.28 后改 proxies=，升级后静默忽略。
- **修法**：
  - pyproject.toml 加 httpx>=0.27,<0.29（与 langchain_* 兼容窗口一致）。
  - 调用点加注释 # httpx 0.27: proxy= ; 0.28: proxies=。
- **测试**：	est_web_search.py 现有测试应通过（固定版本后）。

### 1.9 [Spec-P1-MIG] 迁移脚本归档 + backup gitignore（0.5h）

- _migrate_consolidate_vulns.py 移到 scripts/migrations/vulnscan_consolidate_20260731.py。
- ulnscan_consolidate_backup_20260731_232210.json 加到 .gitignore：ulnscan_consolidate_backup_*.json。
- docs/migrations/2026-07-31_vulnscan_consolidate.md：描述变更、回滚步骤、备份位置。
- 删除 gent.exe、gent-new、
uclei-test/、8 个 .tmp-restart*.ps1（gitignore）。
- **测试**：N/A（一次性脚本已运行过；新增 	ests/data/test_migration_idempotent.py 跑 --dry-run 断言可重复运行）。

---

## 二、阶段 2：可维护性 P1/P2（4-6h）

### 2.1 [S-P1-2] dispatch 拆分 matcher/nuclei 计数器（1h）

- src/orchestration/subgraphs/vulnscan/nodes.py:206-216：global 引擎下 esult 计数器混 matcher 广播 + nuclei per-agent，operator 看不到哪一边失败。
- **修法**：esult = {"matcher": {...}, "nuclei": {...}}；_pub_progress 报告时也分两条。
- **测试**：	est_vulnscan_nodes.py 加 	est_dispatch_global_separate_counters + 	est_dispatch_nuclei_per_agent。

### 2.2 [S-P1-7] ggregate bulk chunking + 大数据路径（1.5h）

- src/orchestration/subgraphs/vulnscan/nodes.py:218-263：N=10k+ 时单次 bulk 触发 3-5s 阻塞。
- **修法**：
  - 入口预估 unique key 数（	erms 聚合 or len(first_by_key) 上限 10k）。
  - 超 10k 走 streaming：先建 alias ulns-tmp，updates 写到新 index，最后 POST _aliases swap（与 V11 S-P1-8 同步实施）。
  - ulk_update_vulns 内部加 chunk_size=2000 + 重试。
- **测试**：	est_vulnscan_nodes.py 加 	est_aggregate_streaming_path（mock 1w+ findings）。

### 2.3 [S-P1-8 / Spec-P1-NUKE]
uclei_templates._es() 共享 + rebuild_manifest alias-swap（2h）

- src/agents/nuclei_templates.py：
  - 模块级懒加载单例 _es_client: AsyncElasticsearch | None = None，注入或 init 时赋值。
  - ebuild_manifest 改：先 ulk 到
uclei-templates-next，成功 POST _aliases 原子切换，失败保留旧 index。
  - _ingest 的 matched=False publish SSE + audit
uclei_templates_sync_drift。
- **测试**：	est_nuclei_templates.py 加 	est_rebuild_manifest_atomic_swap + 	est_rebuild_manifest_rollback_on_failure + 	est_ingest_drift_alerts。

### 2.4 [S-P1-4 / Spec-P1-FANOUT] sync_*_to_all_agents 抽 _fanout_to_agents（1h）

- src/agents/rules_sync.py：抽 _fanout_to_agents(build_msg, label, send_fn)，sync_rules_to_all_agents 和 sync_nuclei_templates_to_all_agents 都调它。
- **测试**：现有 	est_rules_sync.py 不应受影响；新增 	est_sync_nuclei_templates_uses_fanout 验证 dedup 与 retry 一致。

### 2.5 [V10 留存 P2] nuclei 默认值改空 + Redis 锁清理（0.5h）

- src/common/config/settings.py:127
uclei_download_base_url 默认 ""（与 V10 S-P1-6 一致）。
- src/agents/upgrade.py:224-265 send_to_agent 失败时 wait redis.delete(_nuclei_upgrade_lock_key(agent_id))，让下次 heartbeat 即可重试。
- **测试**：	est_upgrade_service.py 加 	est_nuclei_upgrade_lock_cleaned_on_send_failure + 	est_nuclei_upgrade_noop_when_base_url_empty。

### 2.6 [V10 留存 P2] VulnFilter max_length + 路由层 Pydantic 校验（0.5h）

- src/agents/models.py:VulnFilter cve_keyword/hostname_keyword/
ame_keyword 加 max_length=200（手工 dataclass 校验或在 router 层 Pydantic 包装）。
- **测试**：	est_store.py 加 	est_vuln_filter_rejects_overlong_keyword。

### 2.7 [V10 留存 P1] Go P2 批量（1.5h）

| 项 | 文件:行 | 修法 |
|---|---|---|
| 双 SetRuleVersion | main.go:231,234 | 删除重复调用 |
| DefaultConfigPath vs cfgPath | main.go:216,234,280 | 改用 cfgPath 变量 |
| 二进制 swap 原子性 | upgrade.go:119-132,322-335 | 先 rename tmp→target（原子），再 backup→old（best-effort） |
| PublicKey nil 检查统一 | upgrade.go:49 vs erify.go:62 | 统一用 len(PublicKey) == 0 |
| json.Marshal 错误传播 | erify.go:167-169 | log 错误或 propagate |
| install.sh config.json | install.sh:42 | 加 chmod 600 "/config.json" |
| PublicKey nil erify.go:62 | erify.go:62 | 改 len() == 0 |

- **测试**：main_test.go 加 TestSaveUsesCfgPath（override CONFIG_PATH 后断言写到 override 路径）。

### 2.8 [V10 留存 Spec-P1-RATE] batch-delete max 200（0.5h）

- src/api/routers/vulnscan.py:209-242 if len(req.task_ids) > 200: raise HTTPException(422, "max 200 per request")。
- **测试**：	est_vulnscan_api.py 加 	est_batch_delete_over_200_rejected。

---

## 三、阶段 3：前端卫生 + UX（3-5h）

### 3.1 全局 showError helper（1h）

- 从 UsersPage.tsx:23-28 提取到 rontend/src/utils/showError.ts。
- 替换全站 ~25 处 catch { message.error("固定中文") } 为 showError(err, "固定中文")。
- 统一提取 err.response?.data?.detail 后的 API 错误详情。
- **测试**：rontend/src/utils/__tests__/showError.test.ts（新增）。

### 3.2 columns memoize + useEffect deps 修复（1h）

- ApprovalsPage、EventQueuePage、RulesPage、ScanTaskPage、ScanReportPage 的 columns 数组加 useMemo。
- 修复 ~12 处 useEffect 依赖数组不完整 + eslint-disable 抑制（改用 useCallback + 正确 deps）。
- **测试**：
px tsc --noEmit 无错；
px eslint 警告数从 ~12 降到 0。

### 3.3 s any 类型安全清理（1h）

- AlertInboxPage、EventQueuePage、ScanTaskPage、VulnListPage 的 columns as any → ColumnsType<VulnFinding> 等正确泛型。
- ScanMonitorPage 的 useState<any>(null) → 本地 interface。
- DashboardPage 的 s number 强转 → 修复源头类型。
- **测试**：
px tsc --noEmit 无 ny 警告（grep 检查）。

### 3.4 install.sh token 安全 + NucleiTemplateViewer 优化（0.5h）

- gent/packaging/install.sh:42 chmod 600 "/config.json"（与 2.7 合并）。
- NucleiTemplateViewer.tsx useEffect deps 加 dirty（避免 path 切换时 dirty flag 残留）。

### 3.5 SSE base URL 去重（0.5h）

- DashboardPage、EventQueuePage、EventDetailPage、ScanMonitorPage 的 SSE URL 拼接逻辑提取到 rontend/src/utils/sseBaseUrl.ts。

### 3.6 AiEvidenceBadge 国际化准备（0.5h）

- 硬编码中文 "AI 已处理" / "等待补扫" 移到 i18n dict（即便目前只支持 zh-CN）。
- 前端新建 rontend/src/i18n/zh-CN.ts 统一字典。

---

## 四、阶段 4：测试覆盖补强（6-10h）

### 4.1 Go P1 链路补测（3h）

| 链路 | 测试类型 | 优先级 |
|---|---|---|
| portdiscovery_test.go | 单元：mock ss/proc 成功+失败 | HIGH |
| unner_test.go 加 nuclei ports 路径 | 单元：intersect/filter | HIGH |
| upgrade_test.go 加 size limit / CA fail / restart fail | 单元：mock HTTP | HIGH |
| main_test.go 加 CONFIG_PATH 覆盖 | 单元 | MEDIUM |
| erify_test.go PublicKey nil 边界 | 单元 | MEDIUM |

### 4.2 Python 关键链路补测（3h）

| 链路 | 测试类型 | 优先级 |
|---|---|---|
|
odes.py:aggregate reconcile 流（真实 ES 或 fakeredis + mock store） | 单元 | HIGH |
|
odes.py:dispatch global 分流 | 单元 | HIGH |
|
odes.py:_pub_progress 共享 Redis | 单元 | HIGH |
|
uclei_templates._es() 共享客户端 | 单元 | HIGH |
|
uclei_templates.rebuild_manifest alias swap | 集成（fakeredis） | MEDIUM |
| _migrate_consolidate_vulns.py 幂等 | 单元（dry-run） | MEDIUM |
| chat._classify 异常路径 | 单元 | HIGH |
| chat web_search httpx proxy 兼容 | 单元 | LOW |
| udit_logger.count_for_user rename 后回退 | 单元 | LOW |

### 4.3 前端测试起步（2-3h）

- 新建 rontend/src/utils/__tests__/showError.test.ts。
- 新建 rontend/src/components/__tests__/AiEvidenceBadge.test.tsx（快照测试）。
- 扩展 rontend/e2e/app.spec.ts：新增 nuclei templates tab + batch-delete + chat web search fallback + port discovery scan。

### 4.4 集成测试（2h）

- 	ests/integration/test_vulnscan_reconcile_e2e.py：扫描 1 个 host + 1 个 cve + 重扫，验证 aggregate 自动修复 + scan_history 合并。
- 	ests/integration/test_nuclei_templates_e2e.py：导入 zip + 搜索 + 编辑 + 同步到 agent + 重导入幂等。

---

## 五、阶段 5：继承自更早版本的开放问题

### 5.1 SSE 路由顺序 bug（V9 遗留）

GET /api/v1/events/stream 被 GET /api/v1/events/{event_id}/stream 遮蔽，FastAPI 按注册顺序匹配。
- **修法**：调整 stream.py 中两条路由的注册顺序。
- **测试**：	est_events_stream_not_masked_by_event_id_stream。

### 5.2 VulnFilter ES wildcard 查询性能（V10 遗留）

store.py 中 keyword 查询使用 wildcard + e.escape，大量数据下性能不佳。
- **修法**：ES mapping 显式声明 .keyword 子字段，查询走 	erm / match_phrase_prefix。
- **测试**：	est_store.py 加 	est_vuln_filter_keyword_uses_keyword_subfield。

### 5.3 nuclei 下载走 HTTPS（V10 遗留）


uclei_download_base_url 默认值空后，长期方案是部署内网 HTTPS + sha256 校验。
- **修法**：Nacos 加 NUCLEI_SHA256 字段，Go 端下载后校验 hash；
uclei_templates.py 同样。
- **测试**：upgrade_test.go 加 TestNucleiUpgradeVerifiesSHA256 + 	est_nuclei_templates_import_verifies_sha。

---

## 六、验收门禁

| 批次 | 门禁 |
|---|---|
| 阶段 1 全绿 | V10 留存 P1（nuclei 链）+ V11 新增 P1 全部就位 → 可合并主干 |
| 阶段 1-2 全绿 | 阶段 1 + 可维护性 P1 + Go P2 批量 → nuclei 链整体加固完成 |
| 阶段 1-3 全绿 | 全栈一致 → 可发布候选 |
| 阶段 1-4 全绿 | 测试覆盖达 80% + 关键链路有测试 → 正式发布 |

每批次结束后更新 docs/项目审查V11.md 对应条目状态。

---

## 七、与 V10 计划的差异

- **V10 计划侧重"V10 阶段 1/2 nuclei 链 P1 闭环"**，共 16 项；本批次**只 2 项真正闭环**（S-P1-1 fire-and-forget + G-P1-4 WebSocket reconnect），其余 14 项移交 V11。
- **V11 新增 8 项 P1**（chat 异常降级、nuclei_templates 大小校验、portdiscovery error、_pub_progress 共享、httpx 版本兼容、aggregate bulk chunking、dispatch 计数器拆分）覆盖 V10 不涉及的 chat / 数据治理 / 大数据路径。
- **总 P1 数：V10 14 项 + V11 8 项 = 22 项**，预计 6-9h + 4-6h = **10-15h** 阶段 1-2 完成。
- **V10 计划的阶段 3（前端正则）部分已并入 V11 阶段 3**，差异不大。

---

## 八、风险与回退

| 风险 | 影响 | 回退 |
|---|---|---|
| _classify 改异常行为可能误伤正常 chat 流量 | 用户 chat 体验下降 | 新增 degraded 字段双写，旧逻辑 1 周灰度后删除 |
| portdiscovery 失败时返回 error 触发 scan_step | 原本"静默完成"变成"显式失败" | operator 教学：distroless 镜像需预装 ss 或映射 /proc |
|
uclei_templates 500MB 上限可能误伤大版本升级 | 13k 模板 ~200MB，500MB 留 2.5x 余量 | 监控 90 天后调整 |
| ggregate 走 alias swap 需要索引创建权限 | 缺权限时 fallback 旧 delete-then-bulk | 检测权限 + 启动期 warn |
| Go io.LimitReader 在 HandleRuleUpdate 限 100MB | 现网 rule pack < 5MB | 监控 30 天后调整 |
| _migrate_consolidate_vulns.py 重命名后 operator 找不到 | 已运行的 operator 知道路径 | README + 链接 docs/migrations/ |
