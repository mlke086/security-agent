# 后续计划 V6（2026-07-22 终版）

> 基于《项目审查V6》的剩余问题，按功能影响优先级排列

## 零、最高优先级（agent 端 P0，影响核心功能）

### 0a. agent_upgrade ack 逻辑修复
**问题**: 成功时 os.Exit(0) 不发 ack，失败时发双重 ack
**工作量**: 10 分钟 | **文件**: `agent/cmd/agent/main.go:221-233`

### 0b. scan_cancel context 接入执行流
**问题**: ctx 被 `_` 丢弃，取消扫描完全失效
**工作量**: 30 分钟 | **文件**: `agent/internal/scan/engine.go:160`

### 0c. HandleUpgrade 下载加认证/CA/超时
**问题**: 用裸 http.Get，不一致于 HandleRuleUpdate
**工作量**: 15 分钟 | **文件**: `agent/internal/updater/upgrade.go:41`

## 一、立即修复（P1，影响核心功能）

### 1. ScanMonitorPage/EventDetailPage SSE cleanup 修复
**问题**: cleanup 在 wireEs/wireSource 内部，EventSource 不关闭泄漏
**工作量**: 30 分钟
**文件**: `frontend/src/pages/ScanMonitorPage.tsx`, `frontend/src/pages/EventDetailPage.tsx`
**方案**: 将 cleanup return 移到 useEffect 最外层（参考 DashboardPage/EventQueuePage）

### 2. VulnListPage 筛选器自动刷新
**问题**: 筛选条件改变不触发 fetchData
**工作量**: 5 分钟
**文件**: `frontend/src/pages/VulnListPage.tsx`
**方案**: 加 `useEffect(() => { fetchData() }, [filterSev, filterStatus])`

---

## 二、近期修复（P2，改善体验）

### 3. 对话式扫描 intent 解析补充 nuclei 字段
**工作量**: 15 分钟
**文件**: `frontend/src/components/ChatScan.tsx:687-703`
**方案**: parseIntentFromSources 返回对象补全 nuclei_severity/tags/templates/timeout_sec

### 4. 主机组删除时清空关联主机 group_name
**工作量**: 20 分钟
**文件**: `src/agents/store.py:270-279`, `src/api/routers/agents.py`
**方案**: delete_group 时 `UPDATE hosts SET group_name=NULL WHERE group_name=$1`

### 5. 删除任务清 Redis 缓存
**工作量**: 10 分钟
**文件**: `src/api/routers/vulnscan.py` 或 `src/agents/store.py`
**方案**: delete_task 时 `redis.delete(f"vulnscan:queue:status:{task_id}")`

---

## 三、择机修复（P3，代码质量/技术债）

### 6. main.py import 顺序
**方案**: 重构 import，configure_logging() 前完成所有 import
**工作量**: 15 分钟

### 7. task_queue mypy strict 错误
**方案**: 加 type: ignore 或修类型 stub
**工作量**: 1-2 小时

### 8. 前端 SSE cleanup 模式统一
**方案**: 4个SSE页面（Dashboard/EventQueue/EventDetail/ScanMonitor）统一用 useRef 模式
**工作量**: 1 小时

---

## 四、生产部署前准备

### 9. 生产 nginx 配置
```
location /api/v1/ {
    proxy_pass http://backend:8000;
    proxy_buffering off;              # SSE 必需
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```
**工作量**: 文档+模板

### 10. Docker/K8s 部署模板
- 后端: uvicorn on 8000，env注入 NACOS_SERVER + 敏感配置
- 前端: nginx serve dist/，反向代理/api到后端
- Agent: systemd service 部署，配置AGENT_CONSOLE_EXTERNAL_URL
**工作量**: 2-4 小时

### 11. Nacos 配置最终确认
- namespace=prod, group=security, dataId=security-agent.yaml
- 62个配置项全部就位
- 热更新验证通过
**状态**: ✅ 已完成

---

## 五、总工作量估算

| 优先级 | 项数 | 估计工时 |
|---|---|---|
| P0 agent端 | 3 | 1 小时 |
| P1 立即 | 2 | 35 分钟 |
| P2 近期 | 3 | 45 分钟 |
| P3 择机 | 3 | 2-3 小时 |
| agent端 P1 | 6 | 3-4 小时 |
| agent端 P2 | 9 | 2-3 小时 |
| 安全修复 | 2 | 1 小时 |
| 生产部署 | 5 | 4-6 小时 |
| **合计** | **33** | **14-20 小时** |

---

## 六、建议执行顺序

1. **今日**: P1-1 + P1-2（35分钟，修复两个核心bug）
2. **本周**: P2-3/4/5（45分钟，改善体验）
3. **下周**: 生产部署准备（Docker/K8s/nginx）
4. **技术债日**: P3-6/7/8

---

## 七、项目运行状态

### 当前服务运行状态
| 组件 | 状态 | 端口/方式 |
|---|---|---|
| 后端API | ✅ 运行中 | 192.168.80.1:8000 |
| 前端Dev | ✅ 运行中 | 192.168.80.1:3000 |
| Agent | ✅ 运行中 | 192.168.80.101, systemd |
| Nacos | ✅ 运行中 | 192.168.80.101:8848 |
| PostgreSQL | ✅ | 192.168.80.101:5432 |
| Elasticsearch | ✅ | 192.168.80.101:9200 |
| Redis | ✅ | 192.168.80.101:6379 |

### 配置文件管理
| 位置 | 内容 |
|---|---|
| **Nacos** prod/security | 全量62个配置项（推荐源） |
| **.env** | Nacos连接 + 兜底默认值 |
| **.env.example** | 模板 |
