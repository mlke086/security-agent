# 后续修复计划 V9（基于项目审查 V9）

> 本计划对 V9 两轴发现**统一去重**后分批（两轴重叠项见 V9「五、两轴重叠项」，计一次）。覆盖范围：本批未提交变更引入的 P1/P2 + 继承自 V8 仍开放的 F2/F3/S2/F6/SP6。
>
> **已在本会话先行处置**：`enroll_token.json` 泄漏（删除 + `.gitignore`），不计入下述批次。

## 零、实施原则

- 一次提交一个 P1（或一组强相关 P2），配独立单元测试；提交前先跑「P0 切片」（取消链路 + 升级链路 + F5 投递）相关测试确认无回归。
- 不在没有测试保护的地方改历史 P0 链路。
- 每修一项更新 V9 对应条目状态（FIXED/部分/回归），保留 commit 与文档对应。
- 修完批次 1 前不建议合并到生产。

---

## 一、阶段 1：消除 P1（阻塞提交）

### 1.1 Spec-P1-F2 对话式聊天合一（6-8h）—— 最优先

- `src/api/routers/chat.py:api_chat`（`:238`）：`_classify` 与后续分派合并为**一次** LLM 调用。scan 分支用结构化输出一次性拿到 `ScanIntent`（已有 schema），web/project/chat 分支直接产出最终回复；删除二次 `chat_completion`/`_answer_*` 中的重复调用。
- `scan_chat.py:api_chat`（`:259`）：reply（`:283`）+ intent parse（`:289`）+ 后台标题（`:301`->`:137`）= 3 次；合并 reply 与 intent 为一次，标题生成移到低优先级后台且可跳过。
- **持久化合一**：`/api/v1/chat` 在 scan 分支（及普通 chat 分支）一次性 append user+assistant 双方到 `scan_conversations`；`ChatSource` 的 intent snippet 落库。删除「scan-execute 确认路径」里第二次 `chatConversation` 的独立 LLM 调用，改为复用已落库的那条回复。
- `frontend/src/api/client.ts:450-476` `chatAssistant`：去掉二次 `chatConversation`，信任 `/chat` 一次性完成；**删除 `:470-475` 虚假注释**并替换为真实说明。
- 测试：新增 `tests/unit/api/test_chat_persistence.py`，断言 `/chat` scan 分支 append 正确条数、且 `chat_completion` mock `call_count==1`；普通 chat 分支同样落库且只 1 次 LLM。

### 1.2 S-P1-1 审计日志可靠化（2-3h）

- `src/api/routers/users.py:81-110` `_audit` 改 `async` 并在所有调用点 `await`；或维护模块级 `_AUDIT_TASKS: set`，`create_task` 后 `add`，回调里 `discard`。
- 删除死分支 `else: loop.run_until_complete(...)`；`asyncio.get_event_loop()` -> `get_running_loop()`（改 await 后即不再需要）。
- 测试：`tests/unit/api/test_users_audit.py`，用 `pytest-asyncio` + 弱引用触发断言审计**确实写入**（mock `audit_logger.log`，`gc.collect()` 后断言被调用）。

### 1.3 S-P1-2 硬删依赖检查 + 审计保链（2-3h）

- `users.py:322-340` `DELETE ?hard=true` 前：检查该 username 是否作为 `actor`/`target` 出现在审计索引 / 是否拥有主机/审批/扫描任务；命中则 409 返回清单，引导先转移或改用软删。
- 审计 `actor`/`target` 由裸 username 增补 `user_id`（如可），使硬删后仍可回溯；或硬删改为「匿名化保留行」（置 username=`deleted:<id>`）而非物理删除。
- 测试：`test_users_hard_delete_refuses_when_dependencies`、`test_soft_delete_keeps_audit_chain`。

### 1.4 S-P1-3 VulnscanStore 单例统一（1-2h，去重项①）

- `vulnscan.py:116-133 / 419-433 / 672-700` 三处 `VulnscanStore()` 改用已在作用域的 `get_vulnscan_store()`；或抽 `list_hosts` 类方法不构造 store。
- 顺带清掉函数内 `from src.agents.store import VulnscanStore` 懒加载。
- 测试：`test_vulnscan_uses_singleton_store`（patch `get_vulnscan_store`，断言未被 `VulnscanStore()` 构造）。

### 1.5 S-P1-4 group 过滤服务端化（2-3h）

- `vulnscan.py:414-437` 把组主机名集合下推为 ES `terms` 过滤（`store.list_vulns` 增 `hostnames: set[str]` 参数，ES query 用 `terms` on `hostname.keyword`），删除「取 200 后内存过滤」。
- 测试：`test_list_vulns_group_filter_no_truncation`，构造 >200 条同组漏洞断言全部返回。

---

## 二、阶段 2：安全加固（P1/P2）

### 2.1 改密 / 禁用失效 JWT（3-4h，去重项⑦）

- JWT 加 `ver`（token version）claim，存 `users.token_version`；`change_own_password` 与 `disable` 时 `token_version+=1`；`get_current_user`/`get_user` 校验 `ver` 匹配，不匹配即 401。
- 测试：`test_password_change_invalidates_old_jwt`、`test_disable_invalidates_jwt`。

### 2.2 最后管理员锁死防护（1-2h）

- `users.py` disable/delete admin 前校验 `COUNT(*) WHERE role='admin' AND disabled=false > 1`，否则 409。
- 测试：`test_cannot_disable_last_admin`。

### 2.3 弱口令策略增强（1-2h）

- `users.py:113-128` `_validate_password` 增复杂度（大小写+数字+符号至少 3 类）、`new!=old`、`Admin123` 等大小写不敏感匹配；`UserCreate`/`ChangePasswordRequest` 同步。
- 测试：`test_password_policy_rejects_weak`。

### 2.4 异常类型匹配（0.5h）

- `users.py:299-302` `if "duplicate" in str(exc)...` 改 `asyncpg.UniqueViolationError`。
- 测试：现有重复用户用例改断言类型。

---

## 三、阶段 3：性能 / 正确性（P2）

### 3.1 host-stats 与 target_groups 去除 N+1（3-4h，去重项⑤⑥）

- `vulnscan.py:653-700` `/host-stats`：改为单次聚合查询（ES `aggs` 按 group 分桶 + 漏洞计数），删除 `list_vulns(limit=10000)` + 每组 `list_hosts`；30s 缓存保留但跨 worker 不共享记入文档。
- `vulnscan.py:118-133` task create 的 `target_groups` 解析：缓存组->主机映射（TTL），或建任务时仅存 group id，runner 端解析。
- 测试：`test_host_stats_uses_aggregation`（断言 ES 调用次数恒定）。

### 3.2 ScanTaskPage / VulnListPage 前端减载（2-3h）

- `ScanTaskPage.tsx:55-74` `refreshAiProgress`：改为单一「任务 AI 进度」端点，或 `Promise.all` 带并发上限；`:60` 去 `as any` 用 `ScanReport`/`VulnFinding`。
- `VulnListPage.tsx:130-140` 文本输入加 300ms 防抖（抽 `useDebouncedValue` 共享 hook，与 HostOnboardPage 复用）；`:18` `any[]` -> `VulnFinding[]`，去 `columns as any`。
- 测试：前端组件测试断言防抖后单次请求。

### 3.3 generate_report 并行 LLM（1h）

- `nodes.py:828-850` 摘要 prompt 与 `ai_overall_advice` prompt 用 `asyncio.gather` 并行；`ai_processed` 字段接通或删除（`:583`）。
- 测试：`test_generate_report_parallel_llm`（mock 两个 `gather`）。

### 3.4 聊天搜索小项（1-2h）

- `web_search.py:155-167` `search_nvd` 以 `settings.nvd_timeout_sec` 为上限。
- `:262-270` 缓存「DDG 不可用」信号，避免每次 8s 超时。
- `:210-214` `re.escape` 字面通配部分。
- 测试：`test_search_with_fallback_skips_dead_ddg`。

---

## 四、阶段 4：代码质量 / 可维护性（P2）

### 4.1 乱码修复（1-2h，去重项②）—— 含用户可见缺陷

- 还原所有损坏 UTF-8：`vulnscan.py:574-588-614`、`models.py:111`、`agents.py:223/498`、`ws_gateway.py:110/112`、`HostOnboardPage.tsx:221-225`（`message.error("????")` **必须还原中文**）、`:638` `` `主`+`机`+`列`+`表` `` 拼接改 `主机列表`、`schema.ts`/`openapi.json` 重新生成。
- 根因：定位经过的编码处理工具，避免再现。
- 验证：`git diff` 目视 + 前端跑一次确认无 `????`。

### 4.2 10 端点错误文案（0.5h，去重项③）

- `agents.py:132/178/228/331/345/380/386/394/527/598` 把无关失败的 `detail="Enroll token mint rate limit..."` 改为对应真实原因（令牌无效/未找到、install 脚本、二进制下载、令牌状态）。
- 测试：扩 `test_agents_api.py` 断言各路径文案。

### 4.3 去重 / 抽取 / 死码（2-3h）

- `nodes.py:446-485/545-566` 抽 `_write_fallback(findings, reason)`；统一 `collected_findings` 表示（dict），删 `isinstance(f,dict)` 死分支。
- `models.py` `RoleLiteral` 与 `jwt.py` `Role` 合并为单一真相源。
- `utils/time.ts:74-76` 删 `formatBeijingShort` 死码或接入。
- `adapter.py:222-244` `current_model_name` 收敛异常处理、减少 Feature Envy；`:226` `import json` 顶层化。
- `rules_sync.py:4` `import asyncio`（去 `_asyncio` 别名）。

### 4.4 格式 / 风格（0.5h，去重项⑧）

- 跑 `pre-commit run --all-files`（ruff-format 修 E302/缩进/W391/文件尾换行）；`store.py:696-704` 尾空行、`vulnscan.py:39-67/165`、`models.py:338-373`、测试文件尾换行。
- `users.py:81-83/78-100`、`jwt.py:17-35` 函数内 import 与未注解参数上移/补注解。

### 4.5 前端组件卫生（2h）

- `UsersPage.tsx:43-54` 错误处理抽 helper；`:74` 编辑自己时 `disabled` Switch 禁用。
- `AppLayout.tsx:69-95` 抽 `ChangePasswordModal`。
- `ChatScan.tsx:579-619` `IntentCard` 改单一 `Set` 追踪 targets，支持移除。
- `VulnDetailDrawer.tsx:157` 透传 `vuln.ai_model` 或删 `aiModel` prop。
- `HostOnboardPage.tsx:238` 大括号格式；`:226-237` 升级轮询改后台/降迭代上限，防卸载后 setState。

### 4.6 ws_gateway 收尾（1h）

- `ws_gateway.py:268-285` `send_to_agent` 补 `try/finally: await r.aclose()`。
- `:268-274` `_deliver_pending` 加幂等/重投安全性注释（响应动作需幂等）。

### 4.7 测试 Shotgun 局部化（0.5h）

- `tests/unit/api/test_vulnscan_api.py` 等：加 autouse fixture 提供默认 `mock_adapter.current_model_name`，避免每测试手写。
- `scripts/test_chat_e2e.py` / `test_search.py` 加 README 注明「需网络+运行中服务，勿进 CI 原样」。

### 4.8 杂项注释（0.5h）

- `upgrade.py:55-62` `_version_key` 加预发布标签注释；`pg.py:47-60` `ALTER...IF NOT EXISTS` 加迁移故事注释。

---

## 五、阶段 5：继承自 V8 的开放问题

### 5.1 F3 Agent config_update 生效（4-6h）

- `agent/internal/comm/client.go` heartbeat loop 引 `c.heartbeatInterval` 与 `time.Ticker.Reset()` 绑定；`config_update` 调 `Reset(newDuration)`，勿起新 goroutine。
- `protection.go` 拆 `SampleInterval`（250ms-1s）与 `CPUPollTimeout`；移除 throttle 流程里无中断 `time.Sleep(5s)`，改 `select{case <-ctx.Done(): case <-time.After(...):}`。
- `ws_gateway.handle_message` 补 `config_update` 分支（当前无）。
- 测试：`agent/internal/comm/client_test.go::TestApplyConfigResetsHeartbeatTicker`。

### 5.2 S2 CI 测试隔离（4-8h）

- `tests/unit/api/*.py` `_auth_headers()` 改 `app.dependency_overrides[require_role]`；`conftest.py` 增 `pg_test_pool` fixture（testcontainers postgresql 或 sqlite-bridge），启动 `init_schema()` + seed 4 角色。
- 删除硬编码 `admin123/analyst123/...`，从 fixture 动态拿 JWT。
- 验证：CI `pytest tests/unit` 完全通过且不依赖外部服务。

### 5.3 F6 / SP6 评估（各 2-4h）

- F6 Nuclei 结果被静默改写 `sys_vuln`：核对 `nodes.py`/`runner.py` 结果归一逻辑，保留 `engine` 来源标识。
- SP6 Nacos 启动顺序与热更新边界：固化 ES/PG 建后 Nacos 配置变更的生效语义，加启动顺序测试。

---

## 六、验收门禁

- 批次 1 全绿 + P0 切片无回归 -> 可合并主线（对话聊天演示前必过）。
- 批次 1-3 全绿 -> 可演示全功能。
- 批次 1-5 全绿 + S2 CI 隔离 -> 随时可演示、可发布候选。
- 每批次结束更新 `docs/项目审查V9.md` 对应条目状态，并视情况产出 V9.x 增量。
