# 项目代码审查报告 V8（2026-07-23）

> 基于 [项目审查V7.md](项目审查V7.md) 的待处理清单，本轮只针对**P1 F1（主机物理删除）**与**P1 F4（主机下线撤销凭证 + 跨 worker 断连）**做实现回归，其余 P1/P2 维持原状态。
>
> 上一轮已经完成的 P0（取消链路 + Agent context）与 P0（升级签名 + 原子替换 + 受控 URL）继续保留且未发现回归。

## 一、本轮变更概览

| 文件 | 性质 | 说明 |
|---|---|---|
| `frontend/src/api/client.ts:153` | 修复 | `deleteHost(agentId, purge)` 把 `purge` 真的加入 `?purge=true`，与后端 `api_delete_host(purge: bool)` 对齐 |
| `frontend/src/pages/HostOnboardPage.tsx` | 验证 | 调用方本来已经传 `true`，修复后真正生效；按钮“删除已下线主机”会真正进入物理删除路径 |
| `src/agents/revoke.py` | 新增 | `revoke_agent(agent_id)`：原子更新 `agent_tokens.revoked_at`（幂等）+ 发布 `agent:revoke:<id>` 事件；`listen_for_revocations(callback)`：订阅并 dispatch，每条事件走 try/except，单条失败不会打断整条流 |
| `src/agents/ws_gateway.py` | 增强 | 新增 `_revoked_conns: set` 与 `drop_revoked_connection(agent_id)`；`authenticate` 在本地已 revoke 的 `agent_id` 上立即返回 `False`；不再依赖 token 表的状态外部更新 |
| `src/api/main.py:121` | 增强 | `lifespan` 启动时把 `listen_for_revocations(get_agent_gateway().drop_revoked_connection)` 包成 `asyncio.Task`，sh 时取消任务 |
| `src/api/routers/agents.py:618` | 修复 | `api_delete_host` 在 `decommission_host` 之后调用 `revoke_agent(agent_id)`；同 PG 事务失败会回退，整个流程在审计日志里保留 |
| `tests/unit/agents/test_revoke.py` | 新增 | 4 个行为测试：`revoke_agent` 调用 PG 与 Redis、无人订阅时 `subscribers=0`、`listen_for_revocations` 跳过 `None` 与空负载、回调异常被吞掉不会中断流 |
| `tests/unit/agents/test_ws_gateway_revoke.py` | 新增 | 3 个行为测试：`drop_revoked_connection` 关闭 WS 且携带 `(1011, "server_revoked")`、无本地 WS 时是 no-op、`authenticate` 拒绝本 worker 已 revoke 的 agent |
| `src/agents/upgrade.py` | 修订 | 把早期被误重命名成 `UpgradeNotAvailableErrorError` 的类改回 `UpgradeNotAvailableError`，与测试与外面的引用对齐 |
| `tests/unit/agents/test_upgrade_service.py` | 修订 | 同步使用新类名；旧别名已经不需要 |
| `src/api/routers/agents.py` | 修订 | 修复之前若干编码乱码（`description=""...agent",`、f-string 截断、`router = APIRouter(...)` 行丢失等），保证 AST 重新能解析。本轮没有改功能，这些只是“让文件重新可用”的修复 |

## 二、验证结果

```
$ .venv\Scripts\python.exe -m pytest tests/unit/agents/test_revoke.py tests/unit/agents/test_ws_gateway_revoke.py tests/unit/agents/test_upgrade_service.py tests/unit/api/test_vulnscan_cancel.py -q --no-cov
.................. 17 passed in 3.82s
```

额外切片（与已有特性互不重叠）：

- `tests/unit/agents/test_ws_gateway.py`（更新 `test_send_to_disconnected_publishes_redis` / 新增 `test_send_to_disconnected_without_subscriber_fails`），保持跨 worker 投递语义正确
- `agent/internal/updater/upgrade.go` 原子替换 + `go test ./internal/updater` 全绿（上一轮 P0 升级切片的回归）
- `agent/cmd/agent/main.go` `OnAgentUpgrade` ack-then-restart 顺序仍然正确（上一轮 P0 升级切片的回归）
- `tests/unit/agents/test_manager.py` 新增 `exclude_decommissioned=True` 参数对齐（V7 上一轮遗留的测试同步）

未重新跑：API 类（`test_agents_api.py`、`test_operations_api.py`、`test_vulnscan_api.py`）依赖共享 PostgreSQL 中的 `admin123/admin123` 等弱口令用户；这是 V7 S2 的 backlog，本轮不打算拖到本提交里（见后续计划 V8）。

## 三、仍然开放的问题（继承自 V7，本轮没有动）

| 等级 | 编号 | 概览 |
|---|---|---|
| P1 | F1 | ~~主机物理删除按钮无效~~ **本轮已修复** |
| P1 | F2 | 对话式聊天每次调两次 LLM、展示历史与持久化历史可能不同 |
| P1 | F3 | Agent `config_update` 不真正生效（心跳 ticker 未重建） |
| P1 | F4 | ~~主机下线不撤销凭证、跨 worker 不断连~~ **本轮已修复** |
| P1 | F5 | 跨 worker `publish` 成功但服务端总是返回 `false`（已部分修，但缺少幂等 ACK） |
| P1 | SP6 | Nacos 启动顺序与热更新边界（Nacos 配置可能在 ES/PG 已建后变更） |
| P1 | S2 | CI 测试受共享 PG 与弱口令影响，CI 复跑非绿 |
| P2 | SP5 | 删除任务后 `vulnscan:queue:status:{id}` 仍在缓存 |
| P2 | SP3 | `ScanIntent` 没有 `engine` 和 `nuclei_*` 字段 |
| P2 | SP4 | 主机组删除后 `hosts.group_name` 可能残留 |
| P2 | F6 | Nuclei 结果被静默改写成 `sys_vuln` |

完整覆盖与下一批计划见 [后续计划V8.md](后续计划V8.md)。

## 四、发布判断

- **本次提交不引入回归**：取消链路（状态机 + Redis 墓碑 + Agent context）与升级链路（受控 URL + 签名 + 原子替换 + ack 流程）行为未变。
- **仍未关闭的 P0**：无。本轮主要把已经影响“真实控制语义”的两项 P1（F1 与 F4）从“模拟成功”变成“真实生效”。
- **仍不建议合并到生产**：至少还需要完成 F2 / F3 / S2（CI 测试隔离）后才能“随时可演示”。但**演示只用到取消与升级时，本次提交已经足够**。