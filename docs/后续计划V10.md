# 后续修复计划 V10（基于项目审查 V10）

> 本计划对 V10 两轴发现**统一去重**后分批（两轴重叠项见 V10「五、两轴重叠项」，计一次）。覆盖范围：V9 工作树尚未交付的 P1/P2（主要在 V9 阶段 4.3 与 5.2 收尾）。
>
> **本会话已先行处置**：`ac8ffb6a`（review-prep：抽出 `ChangePasswordModal`、移除 `vuln.ai_model` 硬编码、清掉 4.7 `_host` 重复块、修 `models.py` `RoleLiteral` E402），不计入下述批次。

## 零、实施原则

- 一次提交一个 P1（或一组强相关 P2），配独立单元测试；提交前先跑「P0 切片」（取消链路 + 升级链路 + F5 投递 + F3 心跳 reset + F6 nuclei 来源）相关测试确认无回归。
- 不在没有测试保护的地方改历史 P0 链路。
- 每修一项更新 V10 对应条目状态（FIXED / 部分 / 回归），保留 commit 与文档对应。
- 修完批次 1 前不建议合并到生产。

---

## 一、阶段 1：消除 P1（阻塞合并）

### 1.1 Spec-P1-FILTER `VulnFilter` dataclass（2-3h）—— 最优先

- `src/agents/store.py:585-640` `list_vulns` 11 个可选 kwargs 抽 dataclass。
- `src/api/routers/vulnscan.py:414-447` 路由层在 query params → `VulnFilter` 之间做转换；store 接受 dataclass。
- 测试：现有 4 个 `TestResultsExtendedFilters` 用例 + 1 个 `test_group_filter_pushes_hostnames_server_side` 改用 dataclass 构造。

### 1.2 S-P1-1 全仓 `asyncio.create_task(` fire-and-forget 排查（1-2h）

- 候选点：`src/orchestration/task_queue/enqueue.py` 的 progress 上报路径、`src/agents/ws_gateway.py` 的 `_record_response_ack` 调用方、`src/orchestration/subgraphs/vulnscan/nodes.py` 的后台 `_pub_progress` 调用方。
- 修法：和 V9 `_audit` 一样改 `async + await`，或维护模块级 `_BG_TASKS: set`，回调里 `discard`。
- 测试：每个候选点新增一个 `gc.collect()` 后断言副作用被观测到。

### 1.3 S-P1-3 `_write_fallback(findings, reason)` 抽取（1h，去重项①）

- `src/orchestration/subgraphs/vulnscan/nodes.py:467-486` 与 `:570-585` 重复块抽公共函数：
  ```python
  async def _write_fallback(findings: list, reason: str) -> None:
      """Mark findings as not-AI-processed with a stable reason string.
      Best-effort: each write is logged on failure but does not raise."""
      ...
  ```
- 顺手清掉 `isinstance(f, dict)` 死分支（V9 4.3 规划）：state 的 `collected_findings` 是 `.model_dump()` dict 列表，去掉防御性 isinstance 后代码更短。
- 测试：`test_llm_analysis_no_adapter_marks_awaiting_rescan` + `test_llm_analysis_batch_failed_marks_awaiting_rescan` 共享一个 `_write_fallback` 断言。

### 1.4 S-P1-2 `ChangePasswordModal` 挂回 `AppLayout`（0.5h）

- `AppLayout.tsx` 加回密码入口按钮（KeyOutlined 图标）+ `<ChangePasswordModal open={...} onClose={...} />`。
- 后端 `POST /api/v1/users/me/password` 已 bump `token_version`（V9 2.1），改密后旧 JWT 失效；前端应在 `changePassword` 成功后清空本地 token 并强制重新登录。
- 测试：组件测试覆盖「修改密码成功后跳转到 /login」。

### 1.5 S-P1-4 `rules_sync.py` 未使用 `Semaphore` 清理（0.5h，去重项⑨）

- `src/agents/rules_sync.py` `:509 sem = asyncio.Semaphore(20)` 实例后未使用 —— 删去，或真正用于 `asyncio.gather` 的并发限制。
- 测试：现有 `test_rules_sync` 用例不应受影响。

---

## 二、阶段 2：Spec 收尾（P1/P2） -- 2026-07-30 全部完成（`8e6f47db`）

> 提交记录：``fix(refactor+security): V10 stage 2 - S2 fixture migration,
> ai_processed wiring, list_users SELECT trim``。本批次同时附带一个
> ``chore(repo): gitignore .venv312 + untrack``（`dbca5f51`），因为
> .venv312 早先被错误跟踪，pip 任何动作都会把 site-packages 写进 diff。

### 2.1 S2 测试文件迁移到 `auth_headers` fixture（2-3h，去重项⑦） -- FIXED

- `tests/unit/api/test_users_api.py`、`test_vulnscan_api.py`、`test_chat_persistence.py`、`test_operations_api.py` 内的 `_login("admin")` + 硬编码 `admin123` 替换为 fixture 注入：
  ```python
  def test_xxx(auth_headers):
      headers = auth_headers("admin")
      ...
  ```
- 配套：删 `tests/unit/api/test_*.py` 顶部的 `passwords = {"admin": "admin123", ...}` 常量字典。
- 测试：原有 13 个 users 用例 + 28 个 vulnscan 用例 + 4 个 chat 用例都迁移完。

### 2.2 `nodes.py` 字段统一（1-2h） -- FIXED

- `8e6f47db` 在 `generate_report` 里把 `ai_processed` 改为读 `state["ai_processed"]`（llm_analysis 已写），并删掉本地 `ai_processed = True` 的两处赋值。

- `llm_analysis` 返回 `ai_processed` 与 `generate_report` 自算 `ai_processed` —— 要么接通（`generate_report` 读 `state["ai_processed"]`），要么删除半孤儿字段（`llm_analysis` 返回值去掉 `ai_processed`）。
- 修法：默认接通 —— `state["ai_processed"] = bool(llm_analysis_result.get("ai_processed"))`。

### 2.3 `list_users` SELECT 字段剪裁（0.5h） -- FIXED

- `8e6f47db` 把 `list_users` 的 SELECT 改为不带 `hashed_password`；UserInDB 重建时传空串。回归测试 `test_list_users_selects_no_hashed_password` / `test_list_users_does_not_load_bcrypt_in_memory` 已加进 `tests/unit/api/test_users_api.py`。

- `src/api/routers/users.py:188` `SELECT username, hashed_password, role, ...` 改为不查 `hashed_password`（路由层不需要，`UserInDB` 重建时传空串）。
- 测试：现有 `test_create_user_audits_and_hard_delete_refused` 等用例断言不被影响。

### 2.4 `enqueue_task` 后台写入路径 await 化（1h） -- FIXED (no-op)

- `enqueue.py` 本就是全程 `await`（XADD / set 都已 await），`grep asyncio.create_task src/orchestration/task_queue/` 零命中。本项无需代码改动，仅核验。

- `src/orchestration/task_queue/enqueue.py` 任何 `asyncio.create_task(...)` 模式改为 `await`。
- 测试：单测覆盖 enqueue 后下游立即可见（不丢任务）。

---

## 三、阶段 3：前端卫生（P2）

### 3.1 `VulnListPage` 类型 + 防抖（1h）

- `pages/VulnListPage.tsx:18` `useState<any[]>([])` → `useState<VulnFinding[]>([])`，删 `columns as any`。
- `:130-140` 文本输入每次击键触发 `fetchData` 改 300ms 防抖 —— 抽 `useDebouncedValue` 共享 hook（`HostOnboardPage` 已有），前端 `utils/` 下新增文件。

### 3.2 `HostOnboardPage` 升级轮询非阻塞化（1h）

- `:226-237` `for i<45 sleep(2000)` 阻塞按钮 90s + 组件卸载后 setState 改为后台轮询（`setInterval` + 卸载 clearInterval + alive 标志）。
- 测试：组件 unmount 后不再 setState。

### 3.3 `ChatScan.handleSend` 已修但 `chatConversation` 双 LLM 仍残留（0.5h）

- `frontend/src/api/client.ts:450-476` `chatAssistant` 内部注释 + `ChatScan.tsx:166-167` 注释已说明不再调 `chatConversation` —— 但代码路径仍在 `ChatScan.tsx` 里保留 `chatConversation(activeId, ...)` 调用（已删除？需确认）。如果还存在，作为 dead code 删除。

---

## 四、阶段 4：代码质量 / 可维护性（P2）

### 4.1 `_write_fallback` 异常处理（与 1.3 合并）

### 4.2 死码 / 注释清理（1h）

- `src/agents/upgrade.py` `_version_key` 注释已加（V9 4.8），但函数体仍只 `re.findall(r"\d+", ...)` —— 加 1 行说明「regex 只看 digits，中文版本号按数字部分排序」。
- `src/agents/ws_gateway.py` `_deliver_pending` 注释（V9 4.6）说「生产者应设置 seq」，但 `send_to_agent` 没真正设置 —— 把注释改为「当前依赖生产者的幂等保证；如需严格 seq 计数，见 issue #XXX」（指向 follow-up，而不是虚承诺）。

### 4.3 `Role` vs `RoleLiteral` Divergent Change 风险文档化（0.5h）

- `src/api/auth/jwt.py:Role` 与 `src/agents/models.py:RoleLiteral`（V9 4.3 收为 re-export）写明：「`Role` 是 source of truth；新代码直接用 `from src.api.auth.jwt import Role` 即可，`RoleLiteral` 仅为兼容旧 import 路径保留」。

### 4.4 格式 / 风格（0.5h）

- 跑 `pre-commit run --all-files`（ruff-format 修 E302/缩进/W391/文件尾换行）；残留的 E402（`models.py:268` / `sigma_rules.py:45` / `rules_sync.py` 中历史未上提的 import）按项目惯例允许（带 `# noqa: E402`）。
- `tests/unit/agents/test_store.py` / `test_upgrade_service.py` 文件尾换行已在 V9 修过，确认 pre-commit 不再触发。

### 4.5 前端组件卫生（2h）

- `UsersPage.tsx:43-54` 错误处理抽 helper（与 3.1 合并的 fixture 配合）。
- `AppLayout.tsx:69-95` 密码修改入口按 S-P1-2 挂回。
- `ChatScan.tsx:579-619` 已修（V9 4.5）。
- `VulnDetailDrawer.tsx:157` 已修（`ac8ffb6a`）。
- `HostOnboardPage.tsx:238` 大括号格式已修（ruff format）。

### 4.6 ws_gateway 收尾（0.5h）

- `ws_gateway.py:268-285` `send_to_agent` 已补 `try/finally: await r.aclose()`（V9 4.6）。
- `:268-274` `_deliver_pending` 注释（V9 4.6）按 4.2 调整承诺。

### 4.7 测试 Shotgun 局部化（V9 4.7 已交付）

- `tests/unit/orchestration/test_vulnscan_nodes.py` 9 处 `mock.current_model_name = MagicMock(...)` 收敛到 `_make_mock_adapter()`（已完成，`ac8ffb6a` 进一步清理了 `_host` 重复块）。
- `scripts/test_chat_e2e.py` / `test_search.py` README 注释（V9 4.7 未交付）—— 加 README 一段说明「需网络 + 运行中服务，勿进 CI 原样」。

### 4.8 杂项注释（V9 4.8 已交付）

- `upgrade.py:_version_key` 注释（已加）。
- `pg.py` schema 迁移故事注释（已加）。

---

## 五、阶段 5：继承自 V9/V10 的开放问题

### 5.1 F6/SP6 已交付（V9 5.3）—— 见 V10 「继承自 V9」表

### 5.2 S2 CI 测试隔离收尾（V9 5.2 fixture 已加，文件迁移见本计划 2.1）

### 5.3 F3 Agent config_update（V9 5.1 已交付）—— Go 测试覆盖新增

## 七、V10 阶段 2 验收记录（2026-07-30）

| 项 | 计划条目 | 状态 | 提交 | 备注 |
|---|---|---|---|---|
| 2.1 | S2 测试文件迁移到 `auth_headers` fixture | FIXED | `8e6f47db` | 4 个 unit/api 文件 100% 迁移；sigs_with_auth == auth_calls 校验通过 |
| 2.2 | `nodes.py` 字段统一 | FIXED | `8e6f47db` | `generate_report.ai_processed` 改读 `state["ai_processed"]` |
| 2.3 | `list_users` SELECT 字段剪裁 | FIXED | `8e6f47db` | 不再 SELECT `hashed_password`；2 个回归测试 |
| 2.4 | `enqueue_task` 后台 await 化 | FIXED (no-op) | `8e6f47db` | `enqueue.py` 全程 await，无需改 |
| chore | `.venv312` gitignore + untrack | FIXED | `dbca5f51` | 19,085 文件 untrack，~475 MB |

**阶段 2 整体验收门禁**：批次 1-2 全绿 → 可演示全功能 + 测试隔离生效。

---

## 六、验收门禁

- 批次 1 全绿 + P0 切片无回归 → 可合并主线
- 批次 1-2 全绿 → 可演示全功能 + 测试隔离生效
- 批次 1-4 全绿 → 随时可演示、可发布候选
- 每批次结束更新 `docs/项目审查V10.md` 对应条目状态，并视情况产出 V10.x 增量。