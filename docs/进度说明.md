# 安全 AI Agent 项目 — 进度说明
 
> 编制日期：2026-07-10
> 当前阶段：Sprint 3（进行中）
> 测试状态：67/67 通过
 
---
 
## 一、总体进度
 
| 阶段 | 完成度 | 说明 |
|------|--------|------|
| Sprint 0：环境对齐与冒烟跑通 | 100% | 依赖安装、配置对齐、连通性检测、冒烟测试 |
| Sprint 1：打通端到端主链路 | 100% | MemoryManager、工具箱、真实子图、入库脚本、GraphRAG |
| Sprint 2：预处理/执行层 | 100% | 单元测试(67个)、Kafka压测脚本、沙箱镜像、集成测试 |
| Sprint 3：漏洞/响应/API/前端 | 60% | S3-1/2/3 完成，S3-4/5 待开始 |
 
---
 
## 二、当前代码库结构
 
### 核心模块
 
| 模块 | 行数 | 描述 |
|------|------|------|
| src/common/config/settings.py | 45 | Pydantic 配置，支持 .env |
| src/common/logging/logger.py | 19 | structlog 日志（JSON/Debug 双模式） |
| src/common/audit/audit_logger.py | 25 | ES 审计日志追加写入 |
| src/orchestration/main_graph/graph.py | ~90 | LangGraph 主图 |
| src/orchestration/subgraphs/investigation/ | 5 文件 | 研判子图：CTI + 调查 LLM |
| src/orchestration/subgraphs/vuln_hunter/ | 5 文件 | 漏洞挖掘子图：生成→lint→沙箱→收敛 |
| src/orchestration/subgraphs/responder/ | 3 文件 | 响应子图：playbook匹配 + HITL + 执行 |
| src/orchestration/memory/manager.py | 73 | MemoryManager：Milvus+Neo4j 双写 |
| src/knowledge/graphrag/ | 3 文件 | GraphRAG：向量+图检索，RRF融合 |
| src/knowledge/tools/ | 5 文件 | 工具注册器 + VirusTotal/OTX/通知 |
| src/knowledge/models/adapter.py | 35 | LLM适配器(Claude/OpenAI/DeepSeek) |
| src/preprocessing/ | 4 文件 | 脱敏引擎、IOC提取器、Kafka消费者 |
| src/execution/sandbox/executor.py | 60 | Docker沙箱执行器 |
| src/execution/linter/poc_linter.py | 61 | PoC静态检查器(语法/import/危险调用) |
| src/api/main.py | ~55 | FastAPI 主应用 |
| src/api/auth/ | 3 文件 | JWT认证 + RBAC四角色 |
| src/api/routers/operations.py | ~60 | 审批/轨迹/指标端点 |
| src/common/celery_app.py | ~20 | Celery 异步任务(审批超时) |
 
---
 
## 三、Sprint 3 完成详情
 
### S3-1：漏洞挖掘子图闭环修复
- 问题：vuln_check_node 传 dict 给子图，但子图期望 VulnHunterMemory 对象
- 修复：在 graph.py 的 vuln_check_node 中引入 VulnHunterMemory，创建实例并传参
 
### S3-2：响应子图
- 10 剧本 YAML：覆盖恶意软件、钓鱼、CVE、勒索、数据泄露、横向移动、DDoS、暴力破解、DNS隧道、未授权访问
- HITL 替代：移除 langgraph.interrupt（0.2.28 不支持），改用内存待审批队列
- Celery 应用：src/common/celery_app.py，含 approval_timeout_task
 
### S3-3：FastAPI 补全
- JWT 认证：/api/v1/auth/login 返回 token，/api/v1/auth/me 获取用户信息
- RBAC 四角色：admin/analyst/viewer/responder
- 审批端点：POST /api/v1/events/{id}/approve
- 轨迹端点：GET /api/v1/events/{id}/trace
- 指标端点：GET /api/v1/metrics
 
---
 
## 四、待办事项（Sprint 3 剩余）
 
### S3-4：前端初始化（未开始）
frontend/ 目录为空，需初始化 React + Ant Design，实现 4 页面：
- 事件队列 / 推理轨迹 / 审批列表 / 运营大屏
 
### S3-5：5 类 E2E 联调场景（未开始）
1. Honeypot → 调查子图 → 研判结论
2. CVE → 漏洞挖掘子图 → PoC 验证
3. 噪音事件 → 忽略路径
4. 高危事件 → 响应子图 → HITL → 执行
5. 认证 + 授权完整流程
 
---
 
## 五、已知技术债
 
| 问题 | 说明 |
|------|------|
| Windows PowerShell 引号转义 | python -c 经常失败，改用脚本文件 |
| langgraph.interrupt 不可用 | 0.2.28 不支持，已用内存队列替代 |
| Docker pip 线程限制 | 使用 --ulimit nproc=4096 构建 |
| 服务器 HTTP 代理 | 使用临时代理 192.168.254.121:7897 |
| 测试覆盖率仅 26% | Sprint 4 集成测试阶段 |
 
---
 
## 六、服务器访问信息
 
- 地址/SSH: SSH access via authorized keys
- 临时代理: http://[PROXY_URL]
- 中间件: Redis/Neo4j/Kafka/ES/Milvus（全部 Docker）
- 沙箱镜像: security-agent-sandbox
- 沙箱网络: sandbox-net (172.28.0.0/16)
 
---
 
## 七、下次继续时的入口
 
启动开发服务器：
```powershell
cd V:\project\security-agent
.venv\Scripts\Activate.ps1
$env:API_SECRET_KEY="change-this-secret-key"
python -m uvicorn src.api.main:app --reload --port 8000
```
 
运行测试：
```powershell
python -m pytest tests/unit/ -v
```
 
继续 Sprint 3 剩余任务：S3-4（前端）→ S3-5（E2E 测试）
