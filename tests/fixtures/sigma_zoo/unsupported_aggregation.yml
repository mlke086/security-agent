title: Aggregation rule (unsupported by MVP parser)
id: aggregation-zoo-001
description: Will be skipped because condition uses aggregation
level: high
logsource:
  product: linux
  category: process_creation
detection:
  selection:
    Image|endswith: '/bash'
  condition: selection | count() > 5
fields:
  - Image
tags:
  - attack.execution
